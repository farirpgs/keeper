---
import { getTheme } from "../../domains/utils/getTheme";
import Layout from "../../layouts/Layout.astro";
import { PlaygroundRoute } from "../../routes/PlaygroundRoute/PlaygroundRoute";

const theme = getTheme({});
---

<Layout
  title={[`Playground`]}
  description={undefined}
  theme={theme}
  withDefaultBanner
  maxWidth="full"
>
  <PlaygroundRoute client:load theme={theme} />
</Layout>
